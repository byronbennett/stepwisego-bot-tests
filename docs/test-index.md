# Test index

Generated by `sgt export-public` — one row per test, with the shared
variables it requires (empty = zero-setup).

| Test | Bot | Requires |
|---|---|---|
| tier1-features/ai/image-audio-artifacts | ai-image-audio-artifacts | `sgtAiOpenAi` |
| tier1-features/ai/structured-extract-schema | ai-structured-extract-schema | `sgtAiOpenAi` |
| tier1-features/ai/text-prompt-mechanics | ai-text-prompt-mechanics | `sgtAiOpenAi` |
| tier1-features/apis/rest-call-localhost | SGT apis — rest-call against a hermetic localhost listener (GET json, POST raw echo, 404 buried, shutdown) | none — runs out of the box |
| tier1-features/bot/run-bot-as-function | SGT bot — run-bot-as-function: echo child round-trips a mapped value, inline AND isolateProcess (forked temp-file channel) | none — runs out of the box |
| tier1-features/bot/run-bot | SGT bot — run-bot: spawn child agent (wait success, wait failure caught by handler, fire-and-forget) | none — runs out of the box |
| tier1-features/clipboard/clipboard-roundtrip | SGT clipboard — set → get → check → datatable → clear → check (real OS clipboard, self-contained) | none — runs out of the box |
| tier1-features/command-line/run-command-basics | SGT command line — run-command (stdout capture, shell redirect witness file, nonzero-exit buried error) [POSIX] | none — runs out of the box |
| tier1-features/cryptography/aes-roundtrip | SGT cryptography — AES-256 string round-trip | none — runs out of the box |
| tier1-features/csv/csv-file-roundtrip | SGT csv — file round-trip (write-line, dt-to-csv, get-csv-contents) | none — runs out of the box |
| tier1-features/csv/csv-inmemory | SGT csv — in-memory (read, to-json, build-line, write-datatable) | none — runs out of the box |
| tier1-features/database/delete-where | SGT — DB delete where | `sgtDbMssql` |
| tier1-features/database/get-schema | SGT database — db-get-schema (tables filter, columns, databases, missing-tableName buried error) | `sgtDbMssql` |
| tier1-features/database/import-csv | SGT database — import-to-database (CSV → SQL Server: sourceField + literal maps, schema-validation buried error) | `sgtDbMssql` |
| tier1-features/database/import-json | SGT database — import-json-to-database (json parm → auto-created SQL Server table, buried schema-validation error) | `sgtDbMssql` |
| tier1-features/database/insert-then-select | SGT — DB insert then select by id | `sgtDbMssql` |
| tier1-features/database/live-mssql-loop-insert | SGT live SQL Server — loop INSERT from SELECT | `sgtDbMssql` |
| tier1-features/database/live-mssql-select-where | SGT live SQL Server — builder SELECT WHERE | `sgtDbMssql` |
| tier1-features/database/live-mysql-loop-insert | SGT live MySQL — loop INSERT from SELECT | `sgtDbMysql` |
| tier1-features/database/live-mysql-select-where | SGT live MySQL — builder SELECT WHERE | `sgtDbMysql` |
| tier1-features/database/live-pg-loop-insert | SGT live PostgreSQL — loop INSERT from SELECT | `sgtDbPg` |
| tier1-features/database/live-pg-select-where | SGT live PostgreSQL — builder SELECT WHERE | `sgtDbPg` |
| tier1-features/database/odbc-roundtrip | SGT database — ODBC trio (modify DDL+DML with ? binds, select with ? bind, stored proc via {CALL}, bad-SQL buried error) | `sgtOdbcMssql` |
| tier1-features/database/select-rows-from-seed | SGT — DB select rows from seed | `sgtDbMssql` |
| tier1-features/database/update-where | SGT — DB update where | `sgtDbMssql` |
| tier1-features/datatable/dt-core | SGT datatable — core reads + select + html (get-column-names, get-row-count, get-value, get-row, select-rows, to-html) | none — runs out of the box |
| tier1-features/email/graph-folders | email-graph-folders | `sgtMailGraph`, `sgtMailGraphUpn` |
| tier1-features/email/graph-roundtrip | email-graph-roundtrip | `sgtMailGraph`, `sgtMailGraphBulkFolderId`, `sgtMailGraphUpn` |
| tier1-features/email/imap-error-paths | email-imap-error-paths | `sgtMailAccount`, `sgtMailImapHost`, `sgtMailImapPort`, `sgtMailPassword`, `sgtMailSmtpHost`, `sgtMailSmtpPort` |
| tier1-features/email/imap-flags-and-bulk-delete | email-imap-flags-and-bulk-delete | `sgtMailAccount`, `sgtMailImapHost`, `sgtMailImapPort`, `sgtMailPassword`, `sgtMailSmtpHost`, `sgtMailSmtpPort` |
| tier1-features/email/imap-folders-and-move | email-imap-folders-and-move | `sgtMailAccount`, `sgtMailImapHost`, `sgtMailImapPort`, `sgtMailPassword`, `sgtMailSmtpHost`, `sgtMailSmtpPort` |
| tier1-features/email/imap-roundtrip | email-imap-roundtrip | `sgtMailAccount`, `sgtMailImapHost`, `sgtMailImapPort`, `sgtMailPassword`, `sgtMailSmtpHost`, `sgtMailSmtpPort` |
| tier1-features/email/smtp-send-matrix | email-smtp-send-matrix | `sgtMailAccount`, `sgtMailImapHost`, `sgtMailImapPort`, `sgtMailPassword`, `sgtMailSmtpHost`, `sgtMailSmtpPort` |
| tier1-features/engine-patterns/end-bot-run-pass | SGT engine-patterns — end-bot-run (pass) stops execution | none — runs out of the box |
| tier1-features/engine-patterns/error-machinery | SGT engine-patterns — error machinery (ignoreErrors, error-handler) | none — runs out of the box |
| tier1-features/engine-patterns/if-else-comparators | SGT engine-patterns — if/else comparator truth table | none — runs out of the box |
| tier1-features/engine-patterns/list-ops-calibration | SGT engine-patterns — list ops calibration (breadcrumb witnesses) | none — runs out of the box |
| tier1-features/engine-patterns/loop-basics | SGT engine-patterns — loop basics (count, fromTo, list, empty, nested, maxIterations) | none — runs out of the box |
| tier1-features/engine-patterns/loop-datatable-subvars | SGT engine-patterns — DataTable/CSV row loops and column sub-variables | none — runs out of the box |
| tier1-features/engine-patterns/loop-signals | SGT engine-patterns — loop signals (while, exit-loop, continue-loop) | none — runs out of the box |
| tier1-features/engine-patterns/token-gbl-var | SGT engine-patterns — {gbl-var:} token pattern (read, pipe functions, write-back via set-variable, var/gbl-var namespaces are distinct) | none — runs out of the box |
| tier1-features/engine-patterns/token-substitution | SGT engine-patterns — token substitution (chaining, verbatim, pipes, missing var) | none — runs out of the box |
| tier1-features/excel/cell-io | SGT excel — cell IO (set/get value, set/get values, clear-values, row/column reads, typing, offsets) | none — runs out of the box |
| tier1-features/excel/data-exchange | SGT excel — data exchange (datatable bridge, jsonl, workbook json, copy-range, clipboard) | none — runs out of the box |
| tier1-features/excel/formulas | SGT excel — formulas (set/get formula, formualizer compute, cached-result-first) | none — runs out of the box |
| tier1-features/excel/range-geometry | SGT excel — range geometry (last-used, first-empty, used-range, region, counts, column letters) | none — runs out of the box |
| tier1-features/excel/save-as-text | SGT excel — text exports (save-as-csv comma/pipe/semicolon, save-as-tab-delimited, escaping, sheet routing) | none — runs out of the box |
| tier1-features/excel/search-format | SGT excel — search & format (find, find-and-replace, sort, dedupe, format-range, clear-formats) | none — runs out of the box |
| tier1-features/excel/sheets | SGT excel — sheets (add, delete, rename, copy, move, activate, names, name-at-index) | none — runs out of the box |
| tier1-features/excel/structure | SGT excel — structure (insert/delete row and column, copy-down literal + formula) | none — runs out of the box |
| tier1-features/excel/workbook-lifecycle | SGT excel — workbook lifecycle (create, save-as-xlsx, save, open, close) | none — runs out of the box |
| tier1-features/files/base64-and-rows | SGT files — base64 roundtrip, row IO, find/replace | none — runs out of the box |
| tier1-features/files/bundled-files | SGT files — bundled files (save-bundled-file, use-bundled-file-as-template) | none — runs out of the box |
| tier1-features/files/file-copy-move | SGT files — copy-file and move-file (dest variants, overwrite, bare-name) | none — runs out of the box |
| tier1-features/files/file-crud-basics | SGT files — CRUD basics (create, write, append, read, exists, delete) | none — runs out of the box |
| tier1-features/files/folder-ops | SGT files — folder ops (create-folder, delete-folder, get-files, get-folders) | none — runs out of the box |
| tier1-features/files/path-metadata | SGT files — path metadata (filename, extension, folder-path, file dates, random filename) | none — runs out of the box |
| tier1-features/files/well-known-folders | SGT files — well-known folders (appdata, desktop, documents — portable shape asserts) | none — runs out of the box |
| tier1-features/files/zip-roundtrip | SGT files — zip roundtrip (zip-files, zip-folder, unzip-file) | none — runs out of the box |
| tier1-features/ftp/exists-checks | SGT ftp — check-file/check-folder exists trichotomy against the loopback sftp server (calibrated sentinel dance, nested create-folder) | none — runs out of the box |
| tier1-features/ftp/failure-modes | SGT ftp — buried failure modes against the loopback sftp server (wrong password, connection refused, missing remote folder on list and download) | none — runs out of the box |
| tier1-features/ftp/folder-lifecycle | SGT ftp — folder lifecycle over loopback sftp (nested create, create-existing pin, listing + wildcard, non-empty recursive delete, delete-missing buried) | none — runs out of the box |
| tier1-features/ftp/listings-and-delete-files | SGT ftp — wildcard listings + delete-files + deleteAfterDownload over loopback sftp (exact surviving sets pinned by listings and the jail) | none — runs out of the box |
| tier1-features/ftp/plain-ftp-folders-and-deletes | SGT ftp — plain-FTP protocol leg: nested create-folder, folder listing, delete-files, recursive delete-folder over a loopback ftp-srv server | none — runs out of the box |
| tier1-features/ftp/plain-ftp-roundtrip | SGT ftp — plain-FTP protocol leg: upload/exists/list/download round trip over a loopback ftp-srv server (unicode filename+content, 0-byte file) | none — runs out of the box |
| tier1-features/ftp/upload-download-roundtrip | SGT ftp — upload/download round trip over a loopback sftp server (unicode filename+content, 0-byte file, directory upload) | none — runs out of the box |
| tier1-features/functions/function-invocation | SGT functions — define + run-function (bindings, defaults, list handle, bad guid) | none — runs out of the box |
| tier1-features/json/json-navigation | SGT json — navigation (parse, get-property, get-values, get-children, stringify) | none — runs out of the box |
| tier1-features/json/json-transform | SGT json — transform (filter, to-csv, to-xml) | none — runs out of the box |
| tier1-features/logic/end-bot-fail | SGT logic — end-bot (endWithFail=true) stops execution and fails the run | none — runs out of the box |
| tier1-features/logic/end-bot-pass | SGT logic — end-bot (pass) stops execution cleanly | none — runs out of the box |
| tier1-features/logic/require-shared-vars-fail | SGT logic — require-shared-vars (behavior=fail) fails the run | `sgtGateMissing` |
| tier1-features/logic/require-shared-vars-pass | SGT logic — require-shared-vars (all present) continues | `sgtGateAlpha`, `sgtGateBeta` |
| tier1-features/logic/require-shared-vars-skip | SGT logic — require-shared-vars (missing/empty) skips cleanly | `sgtGateAlpha`, `sgtGateEmpty`, `sgtGateMissing` |
| tier1-features/logic/structural-logging-delay | SGT logic — section/comment/write-to-log/delay structural + logging actions | none — runs out of the box |
| tier1-features/logic/wait-until-poll-timeout | SGT logic — wait-until (immediate condition, timeout path) | none — runs out of the box |
| tier1-features/regex/regex-matching | SGT regex — matching (is-match, match-all with groups/append) | none — runs out of the box |
| tier1-features/scripts/run-script-node | SGT scripts — run-script node (temp-file exec, argv passing, stdout capture, exit-3 buried error) | none — runs out of the box |
| tier1-features/sharepoint/items-batch-modes | sharepoint-items-batch-modes | `sgtSharePoint` |
| tier1-features/sharepoint/library-files-folders | sharepoint-library-files-folders | `sgtSharePoint` |
| tier1-features/sharepoint/list-lifecycle-roundtrip | sharepoint-list-lifecycle-roundtrip | `sgtSharePoint` |
| tier1-features/sharepoint/select-projection-and-download | sharepoint-select-projection-and-download | `sgtSharePoint` |
| tier1-features/statistics/metrics-recording | SGT statistics — metrics recording (record-metric, increment-counter, record-amount, record-transaction-outcome) | none — runs out of the box |
| tier1-features/user-interactions/choose-from-list-headless | SGT user-interactions — choose-from-list headless contract: unanswerable prompt fails at the bounded deadline (buried), run continues | none — runs out of the box |
| tier1-features/user-interactions/confirm-dialog-headless | SGT user-interactions — confirm-dialog headless contract: unanswerable prompt fails at the bounded deadline (buried), run continues | none — runs out of the box |
| tier1-features/user-interactions/input-dialog-headless | SGT user-interactions — input-dialog headless contract: unanswerable prompt fails at the bounded deadline (buried), run continues | none — runs out of the box |
| tier1-features/user-interactions/message-box-toast | SGT user-interactions — message-box toast mode (autoCloseSeconds>0): non-blocking, run continues immediately, no outputs | none — runs out of the box |
| tier1-features/user-interactions/pickers-headless | SGT user-interactions — choose-file/choose-folder dialog headless contract: no host UI bridge → immediate clean step failure (buried), run continues | none — runs out of the box |
| tier1-features/user-interactions/speak-text-mechanics | SGT user-interactions — speak-text mechanics: volume-0 utterance and invalid voice both complete (speech host never rejects); audio itself unverifiable | none — runs out of the box |
| tier1-features/variables/counters-and-reset | SGT variables — run counters (pass/fail/transaction) + reset-to-default | none — runs out of the box |
| tier1-features/variables/dictionary-ops | SGT variables — dictionary ops (set/overwrite/remove/clear + pipe witnesses) | none — runs out of the box |
| tier1-features/variables/list-hygiene | SGT variables — list hygiene (clear-list, remove-empty-values, sort-email-list) | none — runs out of the box |
| tier1-features/web/browser-tabs | SGT web — browser-tabs (open, count, switch by index/title/url, close, out-of-range buried) | none — runs out of the box |
| tier1-features/web/click-and-keys | SGT web — click-element (single/double/right) + send-keys-browser (Fill, Type unicode, Press to focused element) | none — runs out of the box |
| tier1-features/web/dialogs | SGT web — handle-dialog policies (AcceptAll alert+confirm, DismissAll confirm, AcceptWithText prompt, Off dismisses, lastDialogMessage output) | none — runs out of the box |
| tier1-features/web/downloads | SGT web — download-file (direct fetch, overwrite=false skip, 404 buried) + save-download (browser-triggered attachment, empty-queue timeout buried) | none — runs out of the box |
| tier1-features/web/element-reads | SGT web — get-web-element / get-all-web-elements (unicode innerText, whitespace collapsing, attributes, missing buried, ambiguous buried, list + empty list) | none — runs out of the box |
| tier1-features/web/form-actions | SGT web — perform-browser-action form verbs (Check, Uncheck, SelectOption, ClearField) each witnessed via execute-javascript | none — runs out of the box |
| tier1-features/web/heal-guard-off | SGT web — heal guard OFF: resilienceMode=exactOnly forces stale webElement selectors to FAIL (no silent healing); page state proves nothing landed | none — runs out of the box |
| tier1-features/web/heal-guard-on | SGT web — heal guard ON: healAutoApprove=always heals the same stale webElement selectors via the fuzzy tier and the actions land | none — runs out of the box |
| tier1-features/web/heal-guard-token-actions | SGT web — Token (webElement) selectors now work through perform-browser-action and move-mouse-browser (Track 10 fix): fresh tokens act on the page, stale tokens under exactOnly fail like every other guarded runner | none — runs out of the box |
| tier1-features/web/js-eval | SGT web — execute-javascript marshaling (string, float64, >2^53 loss, object json, array json, null→empty, boolean, awaited promise, dom read, throw buried) | none — runs out of the box |
| tier1-features/web/nav-history | SGT web — perform-browser-action navigation verbs (Reload resets DOM state, GoBack, GoForward) | none — runs out of the box |
| tier1-features/web/page-data | SGT web — get-page-data all five dataTypes (title unicode, url, source, innerText collapses + hides, visibleText raw + includes hidden) | none — runs out of the box |
| tier1-features/web/pointer-actions | SGT web — perform-browser-action Focus/Hover + move-mouse-browser (coordinates then element center re-enter) | none — runs out of the box |
| tier1-features/web/screenshots | SGT web — take-screenshot (Page png, FullPage png, Element png, Page jpeg quality) verified via magic bytes | none — runs out of the box |
| tier1-features/web/session-lifecycle | SGT web — browser session lifecycle (headless open, navigate, page data, close, double-close buried, op-after-close buried) | none — runs out of the box |
| tier1-features/web/upload | SGT web — upload-file into a file input (name, byte size, content read back in-page; missing file buried) | none — runs out of the box |
| tier1-features/web/waits | SGT web — wait-for-element states (Visible appears late, Hidden via removal, Enabled unlocks, Attached immediate, Detached timeout buried, missing Visible timeout buried) | none — runs out of the box |
| tier1-features/xml/xml-file-roundtrip | SGT xml — file round-trip (save-document, open-document) | none — runs out of the box |
| tier1-features/xml/xml-mutate | SGT xml — mutate (new, insert-node, update-node, set-attribute, delete-node, close-document) | none — runs out of the box |
| tier1-features/xml/xml-read | SGT xml — read (parse, get-node, get-children, get-child-values, get-attribute, to-json, stringify) | none — runs out of the box |
| tier2-shapes/int-01-sp-to-excel-report | int-01-sp-to-excel-report | `sgtSharePoint` |
| tier2-shapes/int-02-db-to-csv-to-email | int-02-db-to-csv-to-email | `sgtDbPg`, `sgtMailAccount`, `sgtMailImapHost`, `sgtMailImapPort`, `sgtMailPassword`, `sgtMailSmtpHost`, `sgtMailSmtpPort` |
| tier2-shapes/int-03-rest-json-to-db-roundtrip/chain | SGT INT-03 — rest-json-to-db-roundtrip: listener serves fixture JSON → rest-call → json-parse (lossless) → json-get-property → json-to-datatable → import-json-to-database → SELECT back; big-int/decimal/unicode string-exact, {var:leak} lands literally | `sgtDbMssql` |
| tier2-shapes/int-04-ftp-file-lifecycle/ftp-file-lifecycle | SGT INT-04 — ftp file lifecycle: CRLF/unicode corpus → base64 → zip → sftp upload → download → unzip → base64/text compare + csv parse + remote cleanup by listing | none — runs out of the box |
| tier2-shapes/int-05-excel-loop-db-post/post | SGT INT-05 — excel-loop-db-post (ERP-posting shape): bundled invoice .xlsx → xl-get-range-as-datatable → loop rows → per-row builder INSERT (DECIMAL) → malformed row caught by error-handler → SELECT SUM vs hand constant → posting-summary CSV | `sgtDbMssql` |
| tier2-shapes/int-06-error-handler-retry-chain/retry | SGT INT-06 — error-handler-retry-chain: 500 caught → flag flipped → call-counting /flaky retried to 200 → breadcrumbs exact; ignoreErrors variant buried | none — runs out of the box |
| tier2-shapes/int-07-run-bot-as-function-pipeline/failprop | SGT INT-07 — child-failure propagation: endOnFail=false continues clean; endOnFail=true fails the step (buried with ignoreErrors); status captured false both ways | none — runs out of the box |
| tier2-shapes/int-07-run-bot-as-function-pipeline/pipeline | SGT INT-07 — run-bot-as-function pipeline: seed → child1 transforms list (outputs mapped back) → child2 writes CSV → parent verifies file + values + shared audit trail | none — runs out of the box |
| tier2-shapes/int-08-sp-write-back | int-08-sp-write-back | `sgtSharePoint` |
| tier2-shapes/int-09-xml-transform-file-chain/chain | SGT INT-09 — xml-transform-file-chain: fixture XML → XPath reads → mutate → save → raw byte compare → xml-to-json → json-to-csv → csv-read back | none — runs out of the box |
| tier2-shapes/int-10-email-driven-file-processing | int-10-email-driven-file-processing | `sgtMailAccount`, `sgtMailImapHost`, `sgtMailImapPort`, `sgtMailPassword`, `sgtMailSmtpHost`, `sgtMailSmtpPort` |
| tier2-shapes/int-11-vault-and-global-variable-flow/flow | SGT INT-11 — vault-and-global-variable-flow: {gbl-var:} + {svar:} resolve into REST bearer, DB marker, file path, log; secure-text masking on audit + agent CSV | `sgtDbMssql`, `sgtPathToken` |
| tier2-shapes/int-14-scheduled-shape-smoke/full | SGT INT-14 — scheduled-shape-smoke (full-bot scope): realistic variables/logic/file/csv/database bot, agent-engine canary | `sgtDbMssql` |
| tier2-shapes/int-14-scheduled-shape-smoke/runfrom | SGT INT-14 — scheduled-shape-smoke (runFromStep from Phase B (crumb b:start)): realistic variables/logic/file/csv/database bot, agent-engine canary | `sgtDbMssql` |
| tier2-shapes/int-14-scheduled-shape-smoke/stepchildren | SGT INT-14 — scheduled-shape-smoke (stepAndChildren on the Phase A list loop): realistic variables/logic/file/csv/database bot, agent-engine canary | `sgtDbMssql` |
| tier3-hosted/canary/hosted-canary | SGT tier3-hosted — canary: trivial set-variable proving the hosted plumbing (loader, skip gate, VM trampoline) end to end | none — runs out of the box |
| tier3-hosted/excel-com/print-to-file-port | SGT xlcom-print — unattended print witnessed via the SGT-PDF printer bound to a fixed file port; spool output moved into scratch | `sgtHostedPrintDir` |
| tier3-hosted/excel-com/recalculate-formulas | SGT xlcom-recalculate — real Excel computes a formula the cross-platform save left stale; full and dirty-only paths, literal and variable inputs | none — runs out of the box |
| tier3-hosted/excel-com/refresh-all-mechanics | SGT xlcom-refresh-all — mechanics contract on a connectionless workbook: RefreshAll succeeds, SaveCopyAs and write-back both preserve content | none — runs out of the box |
| tier3-hosted/excel-com/run-macro-echo | SGT xlcom-run-macro — VBA function with args + return value + saveAfter cell write, and a Sub returning empty | none — runs out of the box |
| tier3-hosted/excel-com/save-as-pdf-export | SGT xlcom-save-as-pdf — whole-workbook and single-sheet PDF export witnessed by %PDF magic bytes | none — runs out of the box |
| tier3-hosted/excel-com/save-as-roundtrip | SGT xlcom-save-as — format matrix (csv known-vector from a literal path, legacy xls from a live FULL session) with a COM re-open round-trip witness | none — runs out of the box |
| tier3-hosted/excel-com/session-path-collision | SGT xlcom session-path collision — whole-file op on a literal path that is ALSO a live Full-mode session reuses the session un-owned; the session survives the op | none — runs out of the box |
| tier3-hosted/int-12-clipboard-excel-bridge/clipboard-excel-bridge | SGT INT-12 clipboard-excel-bridge — DataTable through the OS clipboard into a second workbook, null-cell seam pinned, clipboard restored | none — runs out of the box |
