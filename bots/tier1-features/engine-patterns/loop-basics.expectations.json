{
  "test": {
    "id": "tier1-features/engine-patterns/loop-basics",
    "tier": 1,
    "synthesizedAt": "2026-07-08T00:00:00Z",
    "sourceBot": null,
    "vocabularyTargets": {
      "actions": ["loop", "add-to-list"],
      "pipeFns": [],
      "patterns": ["loop", "token:sa"]
    },
    "synthesisModel": null,
    "synthesisRevisions": 0
  },
  "envelope": {
    "ephemeralDbTables": [],
    "fixtureRefs": [],
    "sharePointSandboxLists": []
  },
  "smoke": {
    "expectComplete": true,
    "expectErrorCount": 0,
    "maxDurationMs": 30000
  },
  "predictions": [
    {
      "id": "count-loop",
      "kind": "round-trip",
      "description": "count loop, 3 iterations: loopCount is 1-based, loopIndex is 0-based, and loopItem for count loops is the 0-based index AS A STRING (engine quirk, pinned)",
      "expr": { "equals": [ { "$var": "crumbsCount" }, ["1:0:0", "2:1:1", "3:2:2"] ] }
    },
    {
      "id": "fromto-negative-increment",
      "kind": "round-trip",
      "description": "fromTo 10→2 by -4 counts down inclusive: loopItem 10, 6, 2",
      "expr": { "equals": [ { "$var": "crumbsFromTo" }, ["10", "6", "2"] ] }
    },
    {
      "id": "list-loop",
      "kind": "round-trip",
      "description": "list loop over [x,y,z] preserves order; loopIndex 0-based; loopItem is the element",
      "expr": { "equals": [ { "$var": "crumbsList" }, ["0=x", "1=y", "2=z"] ] }
    },
    {
      "id": "zero-iteration-loop",
      "kind": "round-trip",
      "description": "loop over an EMPTY list runs its children zero times (the executor must not fall back to auto-running unhandled children once)",
      "expr": { "equals": [ { "$var": "crumbsEmpty" }, [] ] }
    },
    {
      "id": "nested-loops",
      "kind": "round-trip",
      "description": "2x2 nested count loops interleave depth-first: outer.inner = 1.1, 1.2, 2.1, 2.2 — inner loop restarts cleanly per outer iteration",
      "expr": { "equals": [ { "$var": "crumbsNested" }, ["1.1", "1.2", "2.1", "2.2"] ] }
    },
    {
      "id": "max-iterations-cap",
      "kind": "round-trip",
      "description": "maxIterations=4 caps a count=10 loop at exactly 4 iterations",
      "expr": { "equals": [ { "$var": "crumbsMax" }, ["1", "2", "3", "4"] ] }
    }
  ]
}
