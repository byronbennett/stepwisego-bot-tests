{
  "test": {
    "id": "tier1-features/engine-patterns/list-ops-calibration",
    "tier": 1,
    "synthesizedAt": "2026-07-08T00:00:00Z",
    "sourceBot": null,
    "vocabularyTargets": {
      "actions": ["add-to-list", "check-value-in-list", "sort-list", "remove-from-list"],
      "pipeFns": [],
      "patterns": []
    },
    "synthesisModel": null,
    "synthesisRevisions": 0
  },
  "envelope": {
    "ephemeralDbTables": [],
    "fixtureRefs": [],
    "sharePointSandboxLists": []
  },
  "smoke": {
    "expectComplete": true,
    "expectErrorCount": 0,
    "maxDurationMs": 30000
  },
  "predictions": [
    {
      "id": "crumbs-order",
      "kind": "round-trip",
      "description": "add-to-list beginning/end ordering: add b(end), c(end), a(beginning) yields exactly [a,b,c] — this is the breadcrumb-witness calibration every engine-pattern triple depends on",
      "expr": { "equals": [ { "$var": "crumbs" }, ["a", "b", "c"] ] }
    },
    {
      "id": "found-b",
      "kind": "round-trip",
      "description": "check-value-in-list returns true for a present item",
      "expr": { "equals": [ { "$var": "foundB" }, true ] }
    },
    {
      "id": "found-missing",
      "kind": "round-trip",
      "description": "check-value-in-list flips a pre-set true to false for an absent item (probe calibration: proves the checker actually writes false, not just leaves defaults)",
      "expr": { "equals": [ { "$var": "foundMissing" }, false ] }
    },
    {
      "id": "case-sensitive-miss",
      "kind": "round-trip",
      "description": "matchCase=true does not match 'B' against list item 'b'",
      "expr": { "equals": [ { "$var": "foundCaseSensitive" }, false ] }
    },
    {
      "id": "case-insensitive-hit",
      "kind": "round-trip",
      "description": "matchCase=false (default) matches 'B' against list item 'b'",
      "expr": { "equals": [ { "$var": "foundCaseInsensitive" }, true ] }
    },
    {
      "id": "sortable-final",
      "kind": "round-trip",
      "description": "sort-list asc is lexical ([banana,apple,cherry] -> [apple,banana,cherry]); remove-from-list by value removes first occurrence; remove-from-list by index is ZERO-based (index 0 removes 'apple') — final list is exactly [cherry]",
      "expr": { "equals": [ { "$var": "sortable" }, ["cherry"] ] }
    }
  ]
}
